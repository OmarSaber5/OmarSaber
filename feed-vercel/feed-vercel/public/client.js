const money=(n)=>new Intl.NumberFormat('ar-EG',{maximumFractionDigits:2}).format(Number(n||0));
const esc=(v)=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
let transactions=[];
function render(rows){document.getElementById('transactions').innerHTML=rows.map(r=>`<tr><td>${esc(r[3])}</td><td>${esc(r[4])}</td><td>${esc(r[5])}</td><td>${money(r[6])}</td><td>${esc(r[7])}</td><td>${money(r[9])}</td><td>${esc(r[10])}</td><td>${esc(r[12])}</td></tr>`).join('')||'<tr><td colspan="8" class="empty">لا توجد عمليات مسجلة.</td></tr>';}
async function load(){const token=new URLSearchParams(location.search).get('token');if(!token){document.getElementById('message').textContent='هذا الرابط غير صالح.';document.getElementById('message').classList.add('error');return}const r=await fetch(`/api/client?token=${encodeURIComponent(token)}`);const data=await r.json();if(!r.ok){document.getElementById('message').textContent=data.error||'تعذر فتح كشف الحساب.';document.getElementById('message').classList.add('error');return}document.title=`كشف حساب ${data.name}`;document.getElementById('name').textContent=data.name;document.getElementById('balance').textContent=money(data.balance);document.getElementById('count').textContent=data.transactions.length;document.getElementById('updatedAt').textContent=`آخر تحديث: ${new Date(data.updatedAt).toLocaleString('ar-EG')}`;transactions=data.transactions;render(transactions);document.getElementById('message').classList.add('hidden');document.getElementById('portal').classList.remove('hidden');}document.getElementById('search').oninput=e=>render(transactions.filter(r=>r.join(' ').includes(e.target.value.trim())));load();
